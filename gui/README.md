# Portway GUI

HeroUI + Vite + React source for the Portway deck.

## Build

From the **repository root**:

```bash
npm run build
```

Or from this folder:

```bash
npm install
npm run build
```

Output goes to `../portway/web/`. `node_modules` stays local and is gitignored.
